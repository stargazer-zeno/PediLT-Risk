# GRPO prompt

These files were exported from `llm/sft/build_sft_dataset.py`, which contains the final probability-only SFT prompt. The final user message is `user_prefix + formatted EHR + user_suffix`.
